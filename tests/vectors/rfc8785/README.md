Test vectors for RFC 8785 (JSON Canonicalization Scheme).

Sourced from https://github.com/cyberphone/json-canonicalization
(testdata/ directory), which is the reference implementation repository
for RFC 8785. Vectors are expressed inline in the test module as
(input, expected_canonical_output) pairs.
