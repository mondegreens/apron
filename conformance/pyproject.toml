[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "apron-conformance"
version = "0.0.0"
description = "Conformance suites and golden fixtures for Apron extension points"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
authors = [{ name = "Vlad Ryzhkov" }]
dependencies = ["pytest>=8"]
